namespace Vtt.Server.Tests;

// Placeholder proving the runner discovers and executes tests in this assembly. Real testing —
// the endpoint tests, the integration harness against Postgres — is task 005 and lands there.
public class TestHarnessTests
{
    [Fact]
    public void TestRunnerExecutesTestsInThisAssembly()
    {
        Assert.True(true);
    }
}
